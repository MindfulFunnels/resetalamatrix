---
import Iframe from "@/components/Iframe.astro";
import Title from "@/components/Title.astro";
import Layout from "@/layouts/Layout.astro";
---

<Layout title='Reset a la Matrix' isHome={true}>
  <!-- Video de fondo estático -->
  <div class='fixed inset-0 z-0 pointer-events-none'>
    <video
      class='w-full h-full object-cover opacity-10 brightness-50'
      autoplay
      loop
      muted
      playsinline
    >
      <source src='/videos/tv-static.mp4' type='video/mp4' />
    </video>
    <div
      class='absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(139,92,246,0.05),transparent_70%)]'
    >
    </div>
  </div>

  <main
    class='flex flex-col items-center px-4 max-w-7xl mx-auto min-h-[calc(100vh-10rem)]'
  >
    <div class='container mx-auto text-center'>
      <div class='max-w-7xl mx-auto'>
        <p class='text-lg lg:text-2xl mb-8 text-gray-300 font-light'>
          <span class='text-purple-400 font-bold'>DESPROGRAMATE</span> de las
          <span class='text-purple-400 font-bold'>MENTIRAS</span> que te contaron.
          Recordá <span class='text-purple-400 font-bold'>QUIÉN SOS</span>, por
          qué estás acá y hacia dónde querés ir. Este no es un curso: es un
          <span class='text-purple-400 font-bold'>DESPERTAR</span>. Una
          invitación a <span class='text-purple-400 font-bold'
            >SALIR DE LA MATRIX</span
          > y
          <span class='text-purple-400 font-bold'>CREAR TU PROPIA REALIDAD</span
          >.
        </p>
      </div>
    </div>
    <div class='relative z-10 text-center space-y-12 w-full'>
      <div class='w-full max-w-5xl mx-auto'>
        <Iframe />
      </div>
      <div>
        <a
          href='/membresia'
          class='group relative px-12 py-8 text-2xl bg-transparent text-purple-400 border-2 border-purple-400 rounded-lg font-bold cursor-pointer overflow-hidden transition-all duration-300 hover:bg-purple-400 hover:text-gray-900'
        >
          <span class='relative z-10'>Conocer más</span>
          <div
            class='absolute inset-0 bg-purple-400 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 origin-left'
          >
          </div>
        </a>
      </div>
    </div>
  </main>
</Layout>
