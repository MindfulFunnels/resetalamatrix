---
interface FAQItem {
  question: string;
  answer: string;
}

const faqItems: FAQItem[] = [
  {
    question: "¿Qué es Reset a la Matrix?",
    answer:
      "Reset a la Matrix es un programa completo diseñado para ayudarte a reprogramar tu mente y alcanzar tus objetivos. Incluye técnicas avanzadas de neurociencia, meditación y desarrollo personal.",
  },
  {
    question: "¿Cuánto tiempo tengo acceso al contenido?",
    answer:
      "La membresía es mensual, trimestral o semestral. Elige el plan que mejor se adapte a tus necesidades, podras disfrutar de todo el contenido de forma durante el tiempo que tengas contratado el plan.",
  },
  {
    question: "¿Necesito experiencia previa?",
    answer:
      "No, el programa está diseñado tanto para principiantes como para personas con experiencia en desarrollo personal. Te guiaremos paso a paso.",
  },
  {
    question: "¿Qué tipo de soporte ofrecen?",
    answer:
      "Ofrecemos soporte por email y acceso a una comunidad privada donde podrás interactuar con otros miembros y recibir ayuda adicional.",
  },
  {
    question: "¿Cómo me registro en la membresía?",
    answer: "Te dejamos un video en el que te explicamos como hacerlo.",
  },
  {
    question: "¿Qué métodos de pago se aceptan?",
    answer: "Aceptamos cualquier tarjeta de crédito o débito.",
  },
  {
    question: "¿Puedo cancelar mi membresía en cualquier momento?",
    answer: "Si, puedes cancelar tu membresía en cualquier momento.",
  },
];
---

<section class='py-8'>
  <div class='container mx-auto px-4 relative'>
    <h2 class='text-5xl font-bold text-center mb-4 text-white'>
      Preguntas Frecuentes
    </h2>
    <p class='text-xl text-gray-300 text-center mb-16 max-w-2xl mx-auto'>
      Resolvemos tus dudas más comunes sobre el programa
    </p>
    <div class='max-w-7xl mx-auto space-y-4'>
      {
        faqItems.map((item, index) => (
          <div class='bg-gray-800/80 backdrop-blur-sm rounded-xl overflow-hidden h-full border border-purple-500/10 hover:border-purple-500/30 transition duration-300'>
            <button
              class='w-full px-6 py-4 text-left flex justify-between items-center hover:bg-gray-700/50 transition-colors'
              aria-expanded='false'
              aria-controls={`faq-${index}`}
            >
              <span class='font-semibold text-white text-lg'>
                {item.question}
              </span>
              <svg
                class='w-5 h-5 transform transition-transform text-purple-400'
                fill='none'
                stroke='currentColor'
                viewBox='0 0 24 24'
              >
                <path
                  stroke-linecap='round'
                  stroke-linejoin='round'
                  stroke-width='2'
                  d='M19 9l-7 7-7-7'
                />
              </svg>
            </button>
            <div id={`faq-${index}`} class='hidden px-6 py-4'>
              <p class='text-gray-300'>{item.answer}</p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<script>
  document.querySelectorAll("button[aria-expanded]").forEach((button) => {
    button.addEventListener("click", () => {
      const isExpanded = button.getAttribute("aria-expanded") === "true";
      button.setAttribute("aria-expanded", (!isExpanded).toString());

      const content = button.nextElementSibling;
      const icon = button.querySelector("svg");

      if (!isExpanded) {
        content.classList.remove("hidden");
        icon.classList.add("rotate-180");
      } else {
        content.classList.add("hidden");
        icon.classList.remove("rotate-180");
      }
    });
  });
</script>
